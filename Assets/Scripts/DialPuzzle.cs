using UnityEngine;

public class DialPuzzle : MonoBehaviour
{
	[SerializeField] GameObject bigDialNeedleObject = null;
	[SerializeField] GameObject smallDialNeedleObject = null;

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
    }
}
